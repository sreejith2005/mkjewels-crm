import {ReferralEntry} from "@/components/referral-entry"; import {ReferralQueue} from "@/components/referral-queue"; import {createClient} from "@/lib/supabase/server";
export default async function ReferralsPage(){const supabase=await createClient();const [{data:profiles},{data:auth}]=await Promise.all([supabase.rpc("get_my_profile"),supabase.auth.getUser()]);const profile=profiles?.[0];if(!profile||!auth.user)return null;const db=supabase as any;const [{data:actor},{data:branches},{data:calling}]=await Promise.all([db.from("users").select("branch_id").eq("id",auth.user.id).single(),db.from("branches").select("id,name").eq("active",true).order("name"),db.from("referral_calling").select("id,status,remark,next_followup_date,converted_client_id,referrals!inner(crm_name,salesperson_id,given_by_client_id,referral_name,referral_number)")]);const rows=calling??[];const clientIds=[...new Set(rows.flatMap((row:any)=>[row.referrals.given_by_client_id,row.converted_client_id]).filter(Boolean))];const [{data:clients},{data:history},{data:users}]=await Promise.all([clientIds.length?db.from("clients").select("client_id,primary_name").in("client_id",clientIds):Promise.resolve({data:[]}),rows.length?db.from("referral_calling_history").select("referral_calling_id,remark,created_at").in("referral_calling_id",rows.map((x:any)=>x.id)).order("created_at",{ascending:false}):Promise.resolve({data:[]}),db.from("users").select("id,name")]);const clientById=new Map<string,any>((clients??[]).map((x:any)=>[x.client_id,x]));const userById=new Map((users??[]).map((x:any)=>[x.id,x.name]));const historyById=new Map<string,any[]>();for(const entry of history??[])historyById.set(entry.referral_calling_id,[...(historyById.get(entry.referral_calling_id)??[]),entry]);const items=rows.map((row:any)=>{const r=row.referrals,h=historyById.get(row.id)??[];return{id:row.id,status:row.status,next_followup_date:row.next_followup_date,remark:row.remark,converted_client_id:row.converted_client_id,followup_count:h.length,crm_name:r.crm_name??"",given_by_client_id:r.given_by_client_id,given_by_name:clientById.get(r.given_by_client_id)?.primary_name??"Client record",referral_name:r.referral_name,referral_number:r.referral_number,salesperson:userById.get(r.salesperson_id)??"",history:h.map((x:any)=>x.remark).filter(Boolean).join("\n")}});return <main className="mx-auto max-w-[1500px] px-5 py-7"><h1 className="text-3xl font-semibold">Referrals Calling</h1><p className="mt-1 text-sm text-stone-600">Live referral leads and calling queue.</p><div className="mt-6"><ReferralEntry role={profile.role} branchId={actor?.branch_id??null} branches={branches??[]}/></div><ReferralQueue role={profile.role} branchId={actor?.branch_id??null} items={items}/></main>}
